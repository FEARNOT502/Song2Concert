node_modules
dist
*.local
.DS_Store

# Audio test assets (large / copyrighted)
*.flac
*.wav

# Redundant source archive
*.zip

# Local Claude Code settings
.claude/settings.local.json

# Design reference material (not app code). Anchored to the repo root: the
# unanchored form also matched src/three/venues/, which is app code.
/venues/
BACKGROUND_SWAP_PROMPT.md
